// SnapDoCore — shared library entry.
// Holds design tokens, mock views (training data), classifier pipeline, and renderer.
// Per classification spec §0.

import Foundation

public enum SnapDoCore {
    public static let version = "0.0.1"
}
